# The main pipeline for databricks_ci_cd
resources:
  pipelines:
    databricks_ci_cd_pipeline:
      name: databricks_ci_cd_pipeline
      catalog: workspace
      schema: databricks_ci_cd_${bundle.target}
      libraries:
        - notebook:
            path: ../src/dlt_pipeline.ipynb

      configuration:
        bundle.sourcePath: ${workspace.file_path}/src
